## How To Run

Create the file `server/.env`

```
BOT_TOKEN=your_token
PORT=your_port
```

Start server:

```
cd server
npm install
npm start
```

Start Web server

```
cd client
npm install
npm run dev
```
